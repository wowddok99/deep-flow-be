# 개발 에이전트 작업 가이드

이 문서는 코드 작업 에이전트가 이 프로젝트를 수정할 때 따라야 할 기준을 정의합니다

## 기본 원칙

* 기존 구조와 스타일 우선
* 불필요한 리팩터링, 파일 이동, 패키지 재구성 지양
* 변경 범위는 요청된 작업에 필요한 수준으로 제한
* 코드 수정 후 최종 응답에 주요 변경 파일과 검증 결과 포함

## 주석 규칙

* 주석은 한국어로 작성
* 종결어미와 문장 끝 마침표 사용 금지
* 개발자가 쉽게 이해할 수 있는 보편적인 단어 사용
* 줄임말보다 도메인과 코드에서 널리 쓰는 용어를 풀어서 사용
* 구현 클래스명이나 프레임워크 용어는 필요할 때만 사용하고, 먼저 도메인 의도를 설명
* 코드만 봐도 알 수 있는 단순 동작 설명 생략
* 무엇을 하는지보다 왜 필요한지 중심으로 작성
* 도메인 의도, 예외 처리, 외부 API, 배치, 점수 계산처럼 판단 근거가 필요한 부분에만 작성
* 주석은 설명 대상 바로 위에 작성
* 특정 분기, fallback, 예외 처리 이유는 해당 분기나 호출 체인 바로 앞에 작성
* 특정 라인에만 해당하는 설명을 메서드 전체 역할처럼 작성하지 않음
* 같은 주제를 설명하는 문장은 불필요한 빈 줄 없이 이어서 작성
* 빈 줄은 설명 주제가 바뀔 때만 문단 구분용으로 사용

## 프로젝트 이해를 돕는 주석 기준

* 처음 보는 개발자가 유스케이스의 목적과 제약을 이해해야 하는 곳에 주석 작성
* 트랜잭션, 분산 락, 동시성, 권한, 중복 방지처럼 순서나 경계가 중요한 코드는 이유와 우회 시 위험을 명시
* 락이나 트랜잭션 설명은 내부 구현명보다 "락 획득 전 트랜잭션 시작 방지", "커밋 전 락 해제 방지"처럼 결과 중심으로 작성
* 별도 빈, 위임, 이벤트, 배치처럼 구조를 나눈 이유가 있으면 어떤 문제를 피하려는지 설명
* 여러 저장소나 외부 포트를 조합하는 서비스 로직은 단순 호출 순서가 아니라 조합 의도를 설명
* 조회 성능을 위해 한 번에 로드하거나 캐시하는 경우 N+1 방지, 중복 조회 방지 같은 판단 근거를 설명
* 도메인 규칙이 코드만으로 바로 드러나지 않으면 예외 조건 바로 위에 규칙의 이유를 설명
* 한 메서드 안에 같은 이유가 반복되면 첫 지점에만 설명하고 이후에는 코드 흐름으로 읽히게 둠
* 주석이 길어질 때는 메서드 내부 주석보다 JavaDoc으로 유스케이스 의도를 먼저 설명

## JavaDoc 기준

* 서비스와 도메인의 주요 public 유스케이스 메서드는 JavaDoc 작성
* 호출자가 알아야 할 역할과 의도 중심으로 작성
* 파라미터와 반환값 설명은 이름만으로 부족할 때만 작성
* 내부 구현 순서, 단순 조건문, 저장소 호출 여부는 작성하지 않음
* 기술적 제약이 호출 방식에 영향을 주는 경우 JavaDoc에 짧게 포함
* 같은 유스케이스 설명 안에서는 빈 줄을 남발하지 않고, 역할과 제약이 다른 경우에만 문단 분리
* 컨트롤러 메서드는 API 의도가 메서드명, 매핑, 요청/응답 타입으로 명확하면 JavaDoc 생략
* 단순 조회, 단순 변환, DTO, enum, repository 메서드는 이름만으로 충분하면 JavaDoc 생략

```java
/**
 * 사용자의 집중 기록을 생성하고 세션 통계에 반영
 *
 * 집중 기록 생성은 사용자 활동 점수와 연동되므로
 * 단순 저장이 아니라 통계 갱신까지 하나의 유스케이스로 처리
 */
public FocusLogResult createFocusLog(CreateFocusLogCommand command) {
    ...
}
```

```java
/**
 * 초대 코드로 크루에 가입
 *
 * 초대 코드 검증만 이 단계에서 수행하고 가입 처리는 크루별 분산 락 안으로 위임
 */
public CrewSummaryInfo joinByCode(Long userId, String code) {
    ...
}
```

## private 주석 기준

* private 메서드는 보조 로직의 판단 근거가 있을 때만 주석 작성
* 단순 변환, 단순 검증, 단순 위임은 주석 생략
* 특정 fallback이나 예외 처리 이유는 해당 분기 바로 위에 작성

```java
// 외부 검색 결과가 없는 경우 최신 세션 기준으로 추천 품질 보정
private List<Session> fallbackToRecentSessions(User user) {
    ...
}
```

## 좋은 주석 예시

```java
// 외부 API 실패 시 기본 응답으로 대체
// 가까운 측정소 기준으로 현재 대기질 조회
// 같은 배치가 동시에 실행되지 않도록 잠금 사용
// 공공 장소 데이터가 부족한 경우 카카오 장소로 보완
// 중복 저장 방지를 위해 외부 데이터 ID 기준으로 갱신
```

## 피해야 할 주석 예시

```java
// 사용자 정보를 조회하는 메서드
// 값을 반환
// null이면 예외 발생
// 리스트를 반복하면서 DTO로 변환
// save 호출
```

## 최종 응답 규칙

* 코드 수정 후 수정된 주요 파일 경로 포함
* 실행한 검증 명령과 결과 포함
* IntelliJ에서 변경이 보이지 않는 경우에만 `Reload from Disk` 또는 `Synchronize` 안내
* IntelliJ Synchronize 단축키는 `Cmd + Option + Y`
* 같은 파일명이 여러 프로젝트에 있을 수 있으므로 프로젝트 루트 경로 확인 안내
